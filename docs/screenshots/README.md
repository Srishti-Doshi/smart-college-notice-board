# Required Screenshots

Add the following screenshots here before final submission:

- `admin-post-view.png`
- `student-feed-filters.png`
- `push-notification-received.png`
- `mobile-view.png`

Suggested capture notes:

- `admin-post-view.png`
  Show the admin workspace with create or edit notice controls visible.

- `student-feed-filters.png`
  Show the student dashboard with at least one active filter chip visible.

- `push-notification-received.png`
  Show an actual browser notification triggered by the app.

- `mobile-view.png`
  Show the responsive version of the dashboard in a narrow viewport.

Important:

- The current project uses browser notifications, not Firebase Cloud Messaging.
- If your evaluator expects FCM-specific evidence, this screenshot set alone will not satisfy that requirement.
